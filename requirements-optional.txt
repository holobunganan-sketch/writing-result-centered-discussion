# Full local document extraction and hybrid retrieval
pypdf>=5.0
python-docx>=1.1
openpyxl>=3.1
python-pptx>=1.0
scikit-learn>=1.4

# Optional OCR; system Tesseract and Poppler are also required
pytesseract>=0.3.13
pdf2image>=1.17

# Optional local embedding backend. The model must already exist in the local cache.
sentence-transformers>=3.0
